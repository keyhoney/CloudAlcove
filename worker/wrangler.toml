name = "cloudalcove"
main = "src/index.ts"
compatibility_date = "2025-04-01"

[[durable_objects.bindings]]
name = "ROOM"
class_name = "RoomDO"

# Workers Free 플랜: SQLite 백엔드 DO만 지원 (new_classes → 유료 플랜)
[[migrations]]
tag = "v1"
new_sqlite_classes = ["RoomDO"]

[dev]
port = 8787
